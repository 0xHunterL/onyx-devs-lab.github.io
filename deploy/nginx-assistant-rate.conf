# Install under /etc/nginx/conf.d/ so these zones are defined in http context.
limit_req_zone $binary_remote_addr zone=assistant_per_ip:10m rate=6r/m;
limit_conn_zone $binary_remote_addr zone=assistant_conn_per_ip:10m;
