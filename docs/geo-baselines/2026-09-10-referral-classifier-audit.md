# GEO 引荐分类器审计 — 2026-09-10

## 结论

新增的 GitHub Actions 链接巡检使用 `Onyx-Buyer-Guide-Link-Check/1.0` 访问带 UTM 的 Onyx URL。旧版引荐脚本没有识别这一内部验证 User-Agent，曾在 `2026-09-10T11:15:12Z` 的临时输出中把巡检请求计入普通归因访问，产生 `77` 次归因、`41` 次“访问者类型未验证”的虚高结果。

该临时结果不作为 GEO 证据，也没有写入公开证据状态或版本化 Release。

## 修正后复测

- 复测时间：`2026-09-10T11:16:39Z`
- 日志范围：自 `2026-09-01T00:00:00Z` 起，包含轮转日志
- 普通归因请求：`31`
- 疑似自动化请求：`24`
- 访问者类型未验证：`7`
- 已排除的合成验证请求：`93`
- 合格 AI 引荐：`0`

修正将 `Onyx-Buyer-Guide-Link-Check` 与 `Onyx-GEO-Release-Check` 一样归类为合成验证流量，并在 `geo:check` 中加入源代码门禁，防止这个 User-Agent 再次污染普通归因统计。

## 证据边界

剩余 7 次请求仍不能直接称为真人访问。其中最新一条带有 `mixuechu.github.io` Referrer，可证明 GitHub Pages 到 Onyx 的站外引荐，但不是 AI 引荐，也不证明搜索收录、AI 引用或非品牌推荐。
