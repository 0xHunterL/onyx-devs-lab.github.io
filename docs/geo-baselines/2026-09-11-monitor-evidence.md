# Onyx GEO 监测证据检查点：2026-09-11

本检查点固定保存生产机自动监测首次稳定运行后的当前证据。监测读取当前 Nginx GEO 日志以及 `.1`、`.2.gz` 数字轮转文件，并启用 OpenAI、Bing、百度、Google、Perplexity、Yandex、Ahrefs、Common Crawl 与 Apple 的提供方核验。

## 当前可复核结果

- 经 OpenAI 官方地址段核验的 GPTBot 正文请求 28 次；最新一次为 `2026-09-12T06:16:18Z` 对首页的成功请求。GPTBot 属训练用途爬虫。纳入 YandexBot 后，全部已核验爬虫合计覆盖 44 个规范正文路径。
- 经核验的 GPTBot 发现文件请求为 14 次；最新一次为 `2026-09-12T06:16:15Z` 对 `sitemap.xml` 的成功请求。此前有 10 次 `2026-09-10` 对 `feed.json` 与机器证据 JSON 的历史请求因分类修正才被识别，不应误报为新发生的抓取。
- 经官方地址段核验的 OAI-SearchBot `robots.txt` 请求累计 10 次，正文请求仍为 0；最新一次发生在 `2026-09-12T06:16:16Z`。它继续证明搜索爬虫能发现本站抓取规则，但没有升级为正文抓取证据。
- 经双向 DNS 核验的 Bingbot 正文请求 7 次；Googlebot 与 Perplexity 正文请求均为 0。
- 依据 Yandex Webmaster 官方方法，对日志中每个 YandexBot 地址执行 PTR 反查，要求主机名以 `.yandex.ru`、`.yandex.net` 或 `.yandex.com` 结尾，并正向解析回原地址。当前及数字轮转日志共识别 71 次已核验正文请求和 64 次发现文件请求，时间范围为 `2026-09-08T12:45:20Z` 至 `2026-09-11T15:31:06Z`，YandexBot 自身覆盖 41 个正文路径。前 132 次属于补齐监测后识别出的历史证据，最新三次才是新发生的抓取；它证明 Yandex 搜索爬虫访问，不证明公开收录、排名、AI 检索或引用。
- 已排除至少 3,088 次带内部标记或经精确审计确认的跨爬虫家族发布自测；Bytespider 正文与发现文件候选均为 0。监测器现会把未来候选独立标记为 `user-agent-only-unverified`；由于 User-Agent 可自报，这类记录不能证明豆包或字节跳动访问。
- 当前 schema version 3 固定 23 条提示词并映射 23 个唯一证据页，补齐招聘、工业 ERP 与债权研究三类场景，同时把品牌案例题映射到全部六个公开案例；较早基线保留原有 20 条协议。其中 15 页曾被已核验爬虫请求，且均有 YandexBot 等搜索相关爬虫证据；18 条提示词至少有一个搜索相关证据页被抓取，12 条的全部证据页均被抓取。分母变化属于协议升版，不是新增抓取；该覆盖也不证明提示词检索、收录、引用、排名或推荐。
- 归因请求 83 次，其中 72 次属于高速／多 User-Agent 协同批量访问、周期轮换客户端访问、内部矛盾的浏览器身份或已知链接扫描，11 次访问者类型未验证；周期轮换客户端模式占 10 次，已知链接扫描占 15 次。另有 6 个被 Cloudflare 邮箱解码路径污染的异常 campaign 值被审计并从归因总量排除。AI Referrer 为 0；未验证访客类型的 UTM 请求不能升级为真人点击。
- 2026-09-11 08:15 UTC 复测唯一标识、品牌词、`site:` 和新 GitHub Discussion 精确标题，未观察到官网或新问答结果，也没有非品牌推荐。品牌查询可复核地返回香港公司注册处、Bloomberg LEI，以及若干使用同一法律名称和登记号的公司目录记录；这些结果只证明法律实体可被外部检索和消歧，不证明官网收录、服务背书或推荐。
- 2026-09-11 12:16 UTC 以同一四组查询再次复测，结果未变：唯一标识、`site:hk.onyxdevslab.com` 和 GitHub Discussion 精确标题均未观察到目标结果，品牌词仍只返回公司注册处、Bloomberg LEI 与第三方法律实体目录。本次负向观察不能证明所有搜索索引都未收录，但足以阻止把当前状态误报为已收录、已检索或已推荐。
- 2026-09-12T07:18:18Z，以唯一标识、限定官网的精确品牌词、排除公司注册处的品牌词及非品牌类目词 `香港 企业 AI 咨询 公司 FDE` 复测。仍未观察到官网或唯一标识结果；品牌词返回 Bloomberg LEI 法律实体记录，非品牌类目结果则明确出现 FDE.HK、FDE100、FDE123、幻实科技、Anyasmart、FDE 工场、韦迪智能、AXiom 与 Alpha Match 等页面，Onyx 官网未出现。机器基线保存了本轮 11 个结果 URL 及边界：这是带时间和查询词的竞争发现集合，不是完整排名、质量评估、独立背书或 AI 答案推荐。没有向豆包发送提示词。
- 2026-09-12T07:21:47Z，生产复采发现日志轮转使当前保留文件中的 YandexBot 正文记录从 71 条暂降为 70 条，尽管第 71 条已被提供方核验并保存在去重账本及事件历史中。采集器现将提供方已核验爬虫指纹作为追加型累计证据：日志退出保留集合后不再倒减累计数，并在 `retentionAdjustments` 显式公开当前日志计数、累计数和新指纹数；引荐、Bytespider 候选、Common Crawl 与 Wayback 等允许重分类、撤回或可用性变化的指标不套用此规则。同期 `CC-MAIN-2026-34` 恢复可用，两个所选索引均返回完整的 0 捕获结果；该范围仍不代表全部 Common Crawl 历史，也不证明搜索或 AI 系统中不存在本站。
- 2026-09-12T07:34:50Z，固定日历任务自动运行成功，所有计数稳定、无新指纹、无可用性变化且发布漂移为同步。revision 35 将 revision 34 已采用的追加型爬虫指纹累计规则直接暴露到公开 `ai-search-evidence-status.json`，包括去重键、当前日志文件数、差异字段、最新差异列表及不适用的指标范围；本地与线上门禁逐字段校验，避免公开累计值缺少可解释的计算口径。这是证据透明度修复，不是新增抓取、收录、引用或推荐。
- 2026-09-12T07:40:51Z，revision 36 将公开累计口径接入生产发布漂移门禁：当前保留日志文件数或 `retentionAdjustments` 与版本化基线不一致时，服务必须失败并要求人工审核，不能继续显示同步。新增两个确定性样例分别验证非空累计差异和日志集合数量变化，GEO 回归测试增至 190 项。该门禁只防止公开解释落后于生产计算，不构成外部可发现性效果。
- 2026-09-12T07:45:09Z，revision 37 将四个生产日志的精确有序路径加入公开累计口径与发布漂移门禁，补上“日志数量相同但来源文件被替换”仍可能漏报的缺口。新增路径替换样例后 GEO 回归测试增至 191 项；它验证来源集合完整性，不构成新的爬虫访问、收录、引用或推荐。
- 2026-09-12T07:51:18Z，revision 38 补齐生产已统计但此前未进入版本化基线的四项提供方核验计数：Googlebot、Perplexity 的发现文件请求，以及 CCBot 的正文／发现文件请求，当前均为 0；同时把完整提供方计数对象发布到机器状态并接入漂移门禁。新增 CCBot 发现文件由 0 变 1 的样例后 GEO 回归测试增至 192 项。该零基线只保证未来变化不会静默漏报，不证明这些爬虫从未访问其他日志或外部系统。
- 2026-09-12T08:05:03Z，再次逐条复测同一四组公开搜索查询。唯一标识和限定官网的品牌词仍未观察到官网结果；排除公司注册处的品牌词返回 Bloomberg LEI、TempB、香港公司数据集与低可信水滴目录等法律实体页面，没有出现 Onyx 官网。非品牌类目词本轮返回的相关集合由上一轮 11 个跨站页面变为 12 个页面，主要由 FDE.HK 首页、产品、企业 AI 诊断、企业 AI 员工、投研助理与方案页组成，另有幻实科技首页；Onyx 官网仍未出现。机器基线保留上一轮时间和结果数，并明确搜索结果集合可波动，本次变化不构成排名趋势、独立背书、AI 答案引用或非品牌推荐。没有向豆包发送提示词。
- 2026-09-12T09:00Z，审计发现无 UTM 的 AI 点击归因只覆盖豆包、ChatGPT、Perplexity、Copilot、Gemini 与 Claude，来自其他主要中文 AI 答案产品的 Referrer 会被遗漏。revision a47847d 按官方当前 Web 入口新增 chat.deepseek.com、kimi.com、yuanbao.tencent.com 与 chat.qwen.ai 四类精确主机匹配，并增加 5 项确定性断言，GEO 回归测试由 192 项增至 197 项。部署后在生产当前及三个数字轮转日志复算，上述四类历史匹配为 0，归因仍为 83 次（72 次疑似自动化、11 次访问者类型未验证），AI Referrer 仍为 0。这项修复只防止未来引荐漏记，Referrer 仍不证明答案引用、非品牌推荐或真人访问；本轮没有向任何 AI 产品发送测试提示词。
- 2026-09-12T09:12Z，以 `Onyx-GEO-Release-Check` 标记的合成请求验证完整 Referrer 链路：公网请求携带 `https://CHAT.DEEPSEEK.COM:443/...`，生产 Nginx 只记录主机 `CHAT.DEEPSEEK.COM`，报告器规范化为 `chat.deepseek.com` 并识别为 DeepSeek，同时将全部 3 条诊断请求排除在真实归因之外。该检查还发现午夜轮转后 Nginx 仍写入 `.log.1`、新主日志为 0 字节；服务器的 `policy-rc.d` 对服务动作固定返回 101，使默认且静默重定向输出的 `invoke-rc.d nginx rotate` 没有执行 reopen。revision 10a913d 将直接 `USR1` 的 logrotate 配置纳入版本控制和 GEO 门禁，生产配置已安装且仓库／系统文件 SHA-256 一致；手动无中断 reopen 后，新合成请求于 `09:15:49Z` 写入主 `.log`，`.log.1` 停止增长。该修复恢复采集连续性，不构成真实 AI 引荐、答案引用、收录或推荐证据。
- 2026-09-12T09:32:44Z，复测 services.hk 的 AI 咨询、ERP 实施和网络安全三个精确详情子域，三页仍全部返回 HTTP `200`，但缺少应有标题、服务描述与联系入口，并命中相同的 PHP `open_basedir`、autoload、`Failed opening required` 和 `Fatal error`，可用数为 0／3。此前这项检查只存在于人工步骤；当前已将精确 URL、正负正文标记、状态聚合和 17 项断言接入六小时生产采集，状态变化会保存为独立可用性事件。它只监测候选分发渠道是否恢复，不证明 Onyx 已提交、收录、审核、背书、搜索索引、AI 引用或推荐；未经当次明确确认仍不发送申请。
- 2026-09-12T09:36:32Z，新监测首次进入正式生产采集并生成 `events/2026-09-12T09-36-32.522Z-evidence-and-availability-change.json`：services.hk 从 `unmonitored` 建立为 `unavailable`，三个精确详情页均为 HTTP `200` 伪成功；该状态现进入公开机器证据和发布漂移门禁。同期 AhrefsBot 从官方 IP 范围访问 `sitemap.xml`，已核验发现文件累计由 82 增至 83，正文仍为 0。Wayback CDX 当前计数由 92 回到 93／43 个 URL，但集合对比证明原因是此前已见的简体定制开发页 `20260909205642` 捕获重新出现；先前消失的 AI 咨询页 `20260909205631` 指纹仍未返回。这是公开索引集合波动，不是新快照、页面恢复、搜索收录或 AI 引用。revision 40 同步上述事实并新增 4 项渠道漂移断言，GEO 回归测试增至 218 项；本轮没有向豆包发送提示词，也没有发送目录申请。
- 2026-09-12T09:47:07Z，revision 40 部署后的复采遇到两个 Common Crawl 索引均返回 HTTP `504`，Wayback CDX 也在两次重试后不可用。可用性事件保存为 `events/2026-09-12T09-47-07.816Z-availability-change.json`，但旧漂移逻辑同时把 Wayback 报告的占位 0 误列为 93→0 条、43→0 个 URL 和提示词归档覆盖归零，并在 0 个可用 Common Crawl 索引时仍标成 `partial`。监测现改为：0 个可用索引必须标 `unavailable`；Common Crawl／Wayback 完全不可用时只比较 availability 和来源集合，不把占位 0 当成证据计数变化。新增 3 项断言后 GEO 回归测试为 221 项；这保留来源故障告警，同时禁止误报历史捕获消失。
- 2026-09-12T10:04:54Z，正式生产复采生成 `events/2026-09-12T10-04-54.078Z-evidence-change.json`，确认 `09:59:36Z` 两个此前未见且命中 Ahrefs 官方 IP 范围的客户端分别访问 `robots.txt` 与 `sitemap.xml`，均返回 HTTP `200`。AhrefsBot 已核验发现文件累计由 83 增至 85，其中 `robots.txt` 22 次、`sitemap.xml` 63 次、77 个不同客户端地址；正文仍为 0。Wayback CDX 同期仍返回 HTTP `503`，因此保留上次可用查询的 93 条／43 个 URL，不用本轮占位 0 覆盖公开基线。这两次请求只证明外部爬虫发现，不证明 Yep 收录、排名、AI 检索、引用或推荐；本轮没有向豆包发送提示词，也没有发送目录申请。
- 2026-09-12T10:17Z，可抓取性审计确认线上 `robots.txt` 对 17 类通用、搜索与 AI User-Agent 明确开放，Sitemap、HTTP 发现链接、缓存校验和内容类型均正常；同时发现精简机器发现文件 `llms.txt` 把 2026-09-10 的不可变爬虫／引荐 Release 标成 `Current`，并在当前值已变化后继续紧邻展示当时的 24／5／7 等旧计数。现改为明确的 `Historical ... 2026-09-10` 检查点，指示机器读取者以实时 AI-search evidence status 为当前来源，并在文件首屏直接加入法律名称、商业登记号和 LEI。离线与线上门禁同时禁止历史检查点重新被误标为当前，并要求三个实体标识始终存在；这项修复改善时间边界与实体消歧，不构成搜索收录、AI 引用或推荐证据。
- 2026-09-12T10:23Z，同一时间边界审计继续发现 `llms-full.txt` 把 2026-09-10 的版本化 AI-search status 资产称为 `current`，且 2026-09-11 revision 7 快照以无时间限定的 `current fixed-prompt crawl coverage` 描述当时覆盖。两处现分别改为带日期的历史资产和“该检查点观察到的覆盖”，README 的旧监测数据也改为“当时保留的 active／rotated 日志”；新增线上与离线断言阻止这些历史标签回退。实时状态文件、历史不可变 Release 和当前日志因此保持三层清晰边界。
- 未向豆包发送提示词。

## 客户端地址证据链加固

- 监测链已改为只在请求确实经过 Cloudflare 时信任还原后的客户端地址。生产 Nginx 使用 Cloudflare 官方公布的 15 个 IPv4 和 7 个 IPv6 地址段；证据报告器同时校验代理地址属于同一份版本化清单。直接访问源站时自报的 `CF-Connecting-IP` 不再能伪造已核验爬虫来源。
- 生产验证中，待旧 Nginx worker 完成优雅退出后，从本机直连并伪造 GPTBot 地址的请求被记录为本机客户端与本机代理地址，没有采用伪造头。经公开 HTTPS 正常进入 Cloudflare 的合成请求，则记录到属于受信 Cloudflare `172.64.0.0/13` 地址段的代理；向公开入口附加伪造地址头的请求由 Cloudflare 返回 403，未进入源站日志。所有合成请求均带 `Onyx-GEO-Release-Check` 标记并被证据统计排除。
- 加固后重新汇总历史生产日志，GPTBot、OAI-SearchBot、Bingbot、Bytespider 和归因请求计数均保持不变。这说明现有基线不依赖可伪造的直连请求头；本项只提高证据真实性，不构成新的抓取、收录、引用或推荐证据。

## 监测源故障隔离

- 2026-09-11T10:59Z 的生产采集因 Common Crawl 官方 CCBot 前缀清单连接超时而失败。报告器现会对 OpenAI、Perplexity 与 Common Crawl 的五个官方前缀源分别重试和记录可用性；单一来源仍失败时，依赖它的候选请求保持 `providerVerified: null`，其余爬虫、归因、提示词覆盖和公开语料库报告继续生成，不能把未核验候选升级为官方访问。
- 2026-09-11T11:03:02Z 用同一个 systemd 服务生产复测成功，五个前缀源均为 `available`，Common Crawl 两个所选索引也全部可用；永久可用性事件为 `events/2026-09-11T11-03-02.897Z-availability-change.json`。GPTBot 27 次正文、OAI-SearchBot 8 次发现文件、Bingbot 7 次正文、Bytespider 0 次非合成候选以及 70 次归因请求的基线均未变化，AI Referrer 仍为 0。
- 2026-09-11T11:23:36Z，生产采集新增 Apple 官方 CIDR 源并成功返回 HTTP `200`；爬虫身份核验覆盖由 5 个来源增至 6 个且全部可用。监测器生成 `events/2026-09-11T11-23-36.089Z-availability-change.json`，明确记录 `crawlerVerification` 的 `coverageChanged:true`、`statusChanged:false`。历史日志中 Applebot 正文和发现文件请求均为 0；GPTBot 27、OAI-SearchBot 正文 0、Bingbot 7、Bytespider 非合成候选 0、归因 70 与 AI Referrer 0 均未改变，因此事件的 `changed` 为 `false`、`newEvidence` 为空。Anthropic 仍未公开可用于核验的稳定 IP 范围，Claude 系列 User-Agent 不升级为已核验证据。
- 2026-09-11T11:32:58Z，生产采集启用 Baiduspider 身份核验：先按百度官方规则确认反向 DNS 主机名以 `*.baidu.com` 或 `*.baidu.jp` 结尾，再正向解析确认回到原 IP；DNS 暂时性故障会标成 `verificationUnavailable`，不会误判为身份失败。当前与轮转日志中 Baiduspider 候选、已核验正文和已核验发现文件均为 0；`changed:false`、`availabilityChanged:false`、`newEvidence:[]`，未生成新事件文件。其余基线保持 GPTBot 正文／发现文件 27／13、OAI-SearchBot 正文／发现文件 0／8、Bingbot 正文 7、归因 70、AI Referrer 0。该结果只证明监测缺口已补齐，不构成百度收录、豆包访问或可发现性提升证据。
- 2026-09-11T12:18:50Z 的生产复采新观察到 2 次 `geo_buyers_guide_scenarios` 归因请求，分别落在法律 AI 案例页和首页，都没有 Referrer，因此只能使“访问者类型未验证”从 12 增至 14，不能计为真人、搜索点击或 AI 引荐。同次 Common Crawl 查询中 `CC-MAIN-2026-30` 可用而 `CC-MAIN-2026-34` 返回 HTTP `504`，可用索引中未观察到捕获，但总状态必须记为 `partial`，不能将 0 解释为两个所选索引的完整零基线。事件保存为 `events/2026-09-11T12-18-50.308Z-evidence-and-availability-change.json`。
- 2026-09-11T12:30:35Z 的生产复采新增 1 次 `geo_buyers_guide_scenarios` 归因请求：12:26:36Z 访问简体中文企业 AI 评估方法页，HTTP `200`，但没有 Referrer，访问者类型未验证。归因总数因此增至 76，其中 61 次疑似自动化、15 次访问者类型未验证；AI Referrer 仍为 0。Common Crawl 的 `CC-MAIN-2026-34` 再次返回 HTTP `504`，总状态继续为 `partial`。事件保存为 `events/2026-09-11T12-30-35.306Z-evidence-change.json`；该事件不证明真人访问、搜索点击、收录、AI 引用或推荐。
- 2026-09-11T12:36:00Z，`CC-MAIN-2026-34` 恢复可查询，与 `CC-MAIN-2026-30` 均返回可用响应；两个所选索引都未观察到官网捕获，因此 Common Crawl 状态由 `partial` 恢复为 `available`。事件保存为 `events/2026-09-11T12-36-00.144Z-availability-change.json`。该完整零结果只覆盖这两个所选索引，不代表全部 Common Crawl 历史，也不证明搜索未收录、AI 未引用或非品牌未推荐；爬虫与归因计数均未变化。
- 同轮审计发现官网公开状态只连接版本化基线、没有直接暴露 Common Crawl 查询状态。公开 `ai-search-evidence-status.json` 现从同一基线生成 `commonCrawlIndexObservation`，并由本地和线上门禁逐字段防止状态、索引范围、捕获数及证据边界陈旧；这是证据可读性修复，不是新增抓取或可发现性效果。
- 生产采集现增加发布漂移后置门禁：将最新 `summary.json` 的核心爬虫、搜索相关提示词覆盖、归因、Common Crawl 索引范围和站外可用性逐项对照仓库版本化基线，报告原子保存为 `/var/lib/onyx-geo/publication-drift.json`。不一致时 systemd 服务显式失败，必须人工审核证据边界后再更新公开状态；它不会自动把未经复核的日志发布到官网。
- 2026-09-11T12:49:36Z，后置门禁首次真实运行即发现发布漂移：12:45:58Z 新增 1 次 `geo_buyers_guide_geo` 请求，访问简体 AI 搜索验证方法页并返回 `200`，但没有 Referrer、访问者类型未验证。归因总数因此增至 77，其中 61 次疑似自动化、16 次访问者类型未验证；AI Referrer 仍为 0。事件保存为 `events/2026-09-11T12-49-36.667Z-evidence-change.json`，漂移报告准确列出公开 76／生产 77 与公开 15／生产 16 两项差异；这不证明真人访问、搜索点击、收录、AI 引用或推荐。
- 2026-09-11T12:59:52Z，后置门禁再次捕获发布漂移：12:56:46Z 新增 1 次 `geo_buyers_guide_scenarios` 请求，访问简体案例证据登记方法页并返回 `200`，但没有 Referrer、访问者类型未验证。归因增至 78 次，其中 61 次疑似自动化、17 次访问者类型未验证；AI Referrer 仍为 0。事件保存为 `events/2026-09-11T12-59-52.694Z-evidence-change.json`；该访问不证明真人、搜索点击、收录、AI 引用或推荐。
- 2026-09-11T13:11:09Z 对 Buyer Guide 请求序列完成周期自动化复核：9 次请求在 91 分钟内覆盖 8 个页面，使用相同旧版 iPhone Safari 身份、无 Referrer、来自 9 个不同可信客户端地址，且 8/8 个相邻间隔均为 4–25 分钟。分类器只有在至少 8 次、6 个页面、6 个可信客户端、至少 80% 间隔落入 4–25 分钟且全部无 Referrer时才标为疑似周期自动化；单一 UA、单一 IP 或固定间隔均不足以触发。重算后 79 次归因中 70 次疑似自动化、9 次访问者类型未验证，周期模式占 9 次，AI Referrer 仍为 0。此前事件文件和已见指纹保留不变；这是证据质量重分类，不是访问减少、收录、引用或推荐证据。
- 2026-09-11T13:37:57Z，固定日历 timer 在首个 `13:30 UTC + 0–10 分钟随机延迟` 窗口自动启动服务，证明调度不再因人工复测而持续后移。采集识别到该轮换客户端序列在 13:34:41Z 新增第 10 次请求：117 分钟内共使用 10 个可信客户端、覆盖 8 个页面、无 Referrer，9 个相邻间隔中 8 个处于 4–25 分钟，因此仍保守归入疑似周期自动化。总归因变为 80 次，其中 71 次疑似自动化、9 次访问者类型未验证，AI Referrer 仍为 0。首次自动采集同时遇到 `CC-MAIN-2026-34` HTTP 504，发布漂移门禁准确阻止旧基线继续标成完整可用；13:39:45Z 使用同一生产服务复采时两个 Common Crawl 索引均恢复可用且仍为 0 捕获，最终漂移只剩上述三项归因计数。该序列不证明真人访问、搜索点击、AI 引荐、引用或推荐；Common Crawl 零结果也只覆盖两个所选索引。
- 2026-09-11T14:00:59Z，新增的 YandexBot 核验层在生产当前与轮转日志中识别出 69 次正文和 63 次发现文件请求，并把固定提示词的搜索相关证据页覆盖从 0 提升到 12／20；15 条提示词至少有一个相关页面被抓取，9 条全部证据页被抓取。`seen-evidence.json` 为 132 条历史与最新 Yandex 观察建立去重指纹，事件保存为 `events/2026-09-11T13-59-28.960Z-evidence-and-availability-change.json`。同轮 `CC-MAIN-2026-34` 连续复测仍返回 HTTP 502，因此 revision 19 如实将 Common Crawl 标为 `partial`；仅可用的 `CC-MAIN-2026-30` 未观察到捕获，不能解释为两个所选索引的完整零结果。YandexBot 抓取不证明 Yandex 搜索收录，更不证明豆包、AI 引用或非品牌推荐。
- 2026-09-11T14:11:42Z，部署 revision 19 后的生产复采中 `CC-MAIN-2026-34` 恢复可用，与 `CC-MAIN-2026-30` 均返回可用响应且未观察到官网捕获。发布漂移门禁正确捕获 `partial` 到 `available` 的三项变化；revision 20 据此记录恢复。完整零结果仍只覆盖两个所选索引，不代表全部 Common Crawl 历史，也不证明搜索未收录、AI 未引用或非品牌未推荐；Yandex、提示词覆盖、归因和站外可用性计数均未变化。
- 2026-09-11T14:32:05Z，新增 AhrefsBot 官方 CIDR 核验后，当前与数字轮转日志共识别 67 次发现文件请求：17 次 `robots.txt`、50 次 `sitemap.xml`，涉及 61 个客户端地址，全部落在官方发布的 81 个 IP 段、返回 HTTP 200；时间范围为 `2026-09-08T13:53:09Z` 至 `2026-09-11T14:24:40Z`。这些请求被写入 `seen-evidence.json`，事件保存为 `events/2026-09-11T14-32-05.739Z-evidence-and-availability-change.json`。AhrefsBot 同时服务 Ahrefs 数据库与 Yep 搜索引擎，因此该结果只证明外部爬虫发现，不证明 Yep 已收录、排名、检索、引用或任何 AI 推荐；正文抓取仍为 0，也不计入固定提示词搜索覆盖。
- 2026-09-11T15:12:09Z，新增 Wayback 官方 CDX 主机级监测。公开查询返回 93 条 HTTP 200、`text/html` 捕获记录，覆盖 43 个不同 URL，捕获时间从 `20260909205420` 至 `20260910164924`。这把此前逐页保存的 11 个永久回放链接扩展为完整、可复测的公开归档清单。同期日志审计发现 98 次自报 `archive.org_bot` 的请求；Archive-It 官方确认该 UA，但未公开可直接核验的 IP 清单，14 个来源地址中 12 个具备 `.archive.org` PTR 并正向回指、2 个无 PTR，因此不把日志请求升级为“提供方已核验”。真正进入证据计数的是公开 CDX 捕获，而不是 UA。历史 404 路径 `/zh-cn/guides/hong-kong-enterprise-ai-providers/` 当前已 301 到现行供应商页，不再是线上缺口。CDX 捕获只证明 Internet Archive 的公开历史归档，不证明当前可访问、搜索收录、AI 检索、引用、排名、背书或推荐；未来 CDX 查询不可用时必须标为 `unavailable`，不能解释成零捕获。
- 2026-09-11T15:18:24Z，Wayback 监测部署后的首轮生产采集同时观察到两次新的 YandexBot 已核验请求：`15:15:14Z` 对 `robots.txt` 的条件请求返回 304，`15:15:15Z` 对英文交付模式对比指南返回 200。YandexBot 累计因此变为正文 70、发现文件 64，Yandex 正文覆盖 41 个路径，全部已核验爬虫合计覆盖 44 个规范正文路径；固定提示词覆盖仍为 12 个证据页、15 条至少命中一个证据页、9 条完全命中。事件保存为 `events/2026-09-11T15-18-24.259Z-evidence-change.json`，其中也首次登记 93 条既有 Wayback 捕获指纹。新增 Yandex 请求证明该爬虫继续访问，不证明公开收录、AI 检索、引用、排名或推荐。
- 2026-09-11T15:44:03Z，生产复采确认 `15:31:06Z` YandexBot 新访问简体 AI 搜索验证方法页并返回 200，Yandex 正文累计增至 71；该路径此前已有其他核验爬虫覆盖，所以全部已核验路径仍为 44，固定提示词覆盖不变。`15:40:22Z` 的中间事件曾把 `15:35:27Z` 与 `15:35:35Z` 两次 Bytespider UA 请求列为身份未核验候选；随后核对命令时间、路径和 UA，确认它们是本轮排查线上门禁瞬时连接失败时由操作员发出的未带标准标记诊断请求。报告器现用精确时间／方法／路径／UA 键把这两条保留日志永久归入合成检查，既不删除历史事件，也不把它们当成豆包证据；复采后 Bytespider 候选回到 0，已排除合成检查为 3,019 次。Wayback 仍为 93 条捕获／43 个 URL，Common Crawl、Wayback 与站外可用性均为 `available`。
- 2026-09-11T15:57:59Z，将 Wayback 的 93 条公开捕获与固定 20 条提示词矩阵逐页比对：20 个唯一证据页中 15 个已有公开历史捕获，15 条提示词至少有一个证据页被归档，12 条提示词的全部证据页均已归档。尚无捕获的是简体中文的香港 AI 咨询公司名单、企业 AI 合作伙伴选择、治理、试点章程和 AI 搜索验证方法五页。对其中一个页面仅尝试一次官方 Save Page Now；接口返回 HTTP 401，明确要求登录，未创建快照。本地与生产均未配置 Internet Archive 凭据，因此没有重试或绕过登录。监测器现自动输出并漂移校验这组覆盖；它只证明公开历史归档，不证明当前可访问、搜索收录、AI 检索、引用、排名、背书或推荐。
- 2026-09-11T16:01:43Z，revision 25 部署后的首轮生产采集发现两条此前未见的 AhrefsBot 已核验发现请求：`15:49:46Z` 访问 `robots.txt`、`15:49:47Z` 访问 `sitemap.xml`，均返回 200 且来源地址命中 Ahrefs 官方 IP 范围。AhrefsBot 发现文件累计从 67 增至 69，其中 `robots.txt` 18 次、`sitemap.xml` 51 次、63 个不同客户端地址；正文请求仍为 0。事件保存为 `events/2026-09-11T16-01-43.357Z-evidence-change.json`，发布漂移门禁按预期阻止旧计数静默通过。Wayback 固定提示词归档覆盖为 15 个证据页、15 条至少命中一页、12 条完全命中，所有监测来源可用。AhrefsBot 同时服务 Ahrefs 与 Yep，因此这只证明发现文件被外部爬虫访问，不证明 Yep 收录、排名、AI 检索、引用或推荐。
- 2026-09-11T16:11:49Z，匿名公网复测仍未在精确品牌、`site:`、类目、验证标记或公开问答查询中观察到官网结果；精确品牌查询新增返回水滴点评网的 `ONYX DEVS LAB LIMITED` 页面。该页的法律名称、成立日期和香港地区与现有记录相符，但页面未认领、缺少登记号和地址，自述数据来自互联网且不作保证，并把行业错误列为“化学原料和化学制品制造业”。因此只把它保存为低可信搜索结果和实体混淆风险，不加入 `sameAs`、不视作权威主体证明，也不升级官网收录、服务验证、AI 引用或推荐状态。公开机器状态现完整暴露本轮查询、URL、冲突字段和处理边界，防止单纯“搜到品牌名”被误报为正向实体信号。
- 2026-09-11T16:43:40Z，对 revision 27 公网门禁后出现的 23 条 Bytespider UA 请求完成正式复采：报告器将它们全部识别为带 `Onyx-GEO-Release-Check` 标记的合成请求，非合成 Bytespider 正文与发现文件候选仍均为 0，未产生新证据事件。同期报告的合成爬虫检查累计为 3,088 次；旧公开字段把它误名为仅 Bytespider 检查且使用陈旧精确值。revision 28 将其迁移为覆盖所有爬虫家族的 `syntheticCrawlerReleaseChecksExcludedMinimum: 3088`。由于每次门禁都会增加累计数，该值只表示版本化时至少已排除的数量，不参与精确发布漂移，也不证明任何外部抓取、收录、AI 引用或推荐。
- 2026-09-12T01:33:05Z 的生产事件新增 6 次 AhrefsBot 发现文件请求、OAI-SearchBot 对 `robots.txt` 的第 9 次已核验请求，以及一条无 Referrer、访问者类型未验证的 Discussion UTM 请求。该轮 CDX 还首次返回 `20260909205631` 的简体 AI 咨询页捕获，使总数短暂变为 93；捕获指纹被永久保存在追加事件中。
- 2026-09-12T06:21:19Z 的下一轮生产事件新增 4 次 AhrefsBot 发现文件请求、GPTBot 对 `sitemap.xml` 与首页的已核验请求各 1 次、OAI-SearchBot 对 `robots.txt` 的第 10 次请求，以及另一条无 Referrer、访问者类型未验证的 Discussion UTM 请求。累计值更新为 GPTBot 正文／发现文件 28／14、OAI-SearchBot 正文／发现文件 0／10、AhrefsBot 发现文件 81、归因 83（疑似自动化 72、访问者类型未验证 11），AI Referrer 仍为 0。
- 同轮可用的 Wayback CDX 查询只返回 92 条、仍覆盖 43 个 URL；不再返回的恰为上一事件的新捕获指纹。随后连续三次独立复测同样得到 92 条，合计四次一致。revision 29 因此发布当前可重复的 92，并保留那条曾见指纹与 93 的历史事件；该减少只能标为公开索引回撤／去重，不能解释为快照、页面、搜索收录或 AI 可发现性消失。
- 2026-09-12T07:01Z 的复采恰逢 Nginx 日志轮转，读取中的当前文件被移至 `.1`，导致一条 `2026-09-11T13:52:33Z` 的 YandexBot `robots.txt` 请求在该次报告中暂时漏读；下一轮从 `.1` 重新读取后发现文件计数恢复为 64，已见指纹未丢失。这是轮转瞬时读竞争，不是爬虫证据减少。同一时段 Common Crawl 两个索引先后出现 404／502；`2026-09-12T07:03:42Z` 独立复测时两个索引均恢复可用且仍未观察到捕获。
- 2026-09-12T07:02:02Z，AhrefsBot 从官方 IP 范围成功访问 `sitemap.xml`，使已核验发现文件累计增至 82，其中 `robots.txt` 21 次、`sitemap.xml` 61 次、74 个不同客户端地址；事件保存为 `events/2026-09-12T07-02-58.975Z-evidence-and-availability-change.json`。revision 30 据此同步最新累计值；正文请求仍为 0，该访问不证明 Yep 或其他搜索／AI 产品已收录、引用、排名或推荐。
- 2026-09-12T07:07:04Z 的完整生产复采中，`CC-MAIN-2026-30` 可用且未观察到捕获，`CC-MAIN-2026-34` 返回 HTTP 504；其间一次独立查询虽短暂恢复，但随后多轮又失败，因此 revision 31 如实发布 `partial`。该零结果只来自一个可用索引，不能解释成两个所选索引或全部 Common Crawl 历史的零捕获；以后恢复为 `available` 时门禁仍会保存可用性转换并要求复核。
- revision 31 首次把非空 `unavailableIndexes` 发布成字符串数组，但漂移器的既有契约要求每项同时保留 `id`、HTTP 状态和原因；这会把索引 ID 比较成 `null`。revision 32 改回结构化对象，并新增回归断言，禁止缺失 ID 的不可用索引基线通过“同步”测试。GEO 回归测试因此由 181 项增至 182 项；这是监测契约修复，不是新的外部可发现性证据。

## 站外分发账本复核

- 版本化分发清单现登记 8 项资产：7 项为已发布并经过匿名核验的 GitHub Gist、GitHub Pages、GitHub Discussion 或 GitHub Release 资产，1 项今日头条原稿保持 `ready-not-published`。所有已发布条目的搜索收录、AI 引用和非品牌推荐字段仍为 `null`。
- 2026-09-11T11:15:50Z 的清单驱动线上门禁请求了 6 个站外公开 URL 和 16 个带归因参数的官网目标；全部返回 HTTP `200`，跳转后地址均未偏离声明目标，公开正文中的品牌、法律主体、主题或活动标记均完整。两轮共 32 次分发门禁请求在生产日志中均被识别为合成访问；11:16 UTC 重新汇总后，有效归因仍为 70 次，其中 61 次疑似自动化、9 次访问者类型未验证，AI Referrer 为 0。该结果证明当前已登记发布物与链接可达且自测未污染基线，不证明搜索收录、AI 引用、推荐或真人访问。
- 2026-09-11T11:19:48Z，正式 `onyx-geo-monitor.service` 首次把同一检查纳入六小时证据采集。`availability.distribution` 为 `available`，22 个来源全部可用，`distribution-live-report.json` 以 `0640 root:root` 持久化；`changed` 与 `availabilityChanged` 均为 `false`，`newEvidence` 为空，有效归因仍为 70、AI Referrer 仍为 0。首次接入只建立站外可用性基线；以后发布物、目标地址或内容标记的状态集合变化才会生成可用性事件，且不会被解释成搜索或 AI 效果证据。
- 2026-09-11T11:57:29Z，生产监测纳入新的搜索监测合规 GitHub Release；分发覆盖从 22 个来源增至 24 个（7 个站外公开 URL、17 个官网归因目标），全部返回 HTTP `200`、未偏离声明目标且内容标记完整。事件 `events/2026-09-11T11-57-29.192Z-evidence-and-availability-change.json` 记录 `coverageChanged:true`、`statusChanged:false`。同次采集新增 3 次带 `geo_buyers_guide_scenarios` 参数、无 Referrer 的请求，有效归因因此增至 73，其中 12 次仅能标为“访问者类型未验证”。这 3 次请求不证明真人访问、搜索点击、AI 引用或非品牌推荐；AI Referrer 仍为 0。

机器可读数据见 [`2026-09-11-monitor-evidence.json`](./2026-09-11-monitor-evidence.json)。这些数字只区分可访问、已核验抓取候选及归因请求，不证明搜索收录、AI 检索、引用、排名、真人访问或非品牌推荐。

- 2026-09-12T10:30:35Z，将 revision 41 的当前公开基线发布为不可变 GitHub Release [`geo-monitor-evidence-2026-09-12-41`](https://github.com/0xHunterL/onyx-devs-lab.github.io/releases/tag/geo-monitor-evidence-2026-09-12-41)，目标精确锁定提交 `fec7283c649ecdbdd8e7b100733e50fe12c5cd69`。Release 中的 `2026-09-11-monitor-evidence.json` SHA-256 为 `cf1a91ce4f40fbc98155163eadacf802f9cf50fc0ce51b55995f0289d18f6e36`，`ai-search-evidence-status.json` SHA-256 为 `5e300f95574024b706c53e284fb117362eec14285f4788f2d4d7f7bee7e1d449`；下载复核与本地发布源逐字节一致。快照记录 GPTBot 正文／发现文件 28／14、OAI-SearchBot 0／10、Bingbot 7／1、YandexBot 71／64、AhrefsBot 0／85，最后一次完整 Wayback 观测为 93 条捕获／43 个 URL，services.hk 三个目标均为 PHP 应用错误。为避免公开客户端地址和日志级细节，未上传 518,781 字节的生产事件原文；只发布经过边界整理的两份 JSON。该 Release 是提供方维护的可复测证据，不证明搜索收录、AI 引用、排名、背书、真人访问或非品牌推荐；未向豆包发送提示词。

- 2026-09-12T10:49:52.764Z，revision 41 部署后的生产采集确认新增 GitHub Release 分发项及其归因目标均可匿名访问：分发清单现有 9 项，其中 8 项已发布、1 项保持 `ready-not-published`，8 个公开 URL 和 18 个归因目标合计 26 个来源全部可用。同期 Wayback CDX 主机查询超时，因此 revision 42 将其当前状态标为 `unavailable`，同时明确保留 09:36:31Z 最后完整观测的 93 条捕获／43 个 URL，未用零值覆盖历史证据。事件保存为 `events/2026-09-12T10-49-52.764Z-availability-change.json`；该事件只记录分发覆盖与外部服务可用性变化，不证明搜索收录、AI 检索、引用、排名、真人访问或非品牌推荐。

- 2026-09-12T11:00:31Z，按固定四查询协议复测公开搜索结果。精确验证标记和 `site:hk.onyxdevslab.com` 查询仍未返回官网；品牌查询仍返回 Bloomberg LEI、tempb、水滴点评和 databasesets 等既有第三方主体记录，未出现新的权威官网归属信号。非品牌查询 `香港 企业 AI 咨询 公司 FDE` 的可见结果集合从上一检查点的 12 条扩展到 23 条，仍无 Onyx，并新增 HKEX 公告、券商研报、InvestHK 档案和百科条目等宽泛或低相关结果。revision 43 保存该竞争发现快照，同时明确结果数量不等于类目相关性，单次集合变化也不构成稳定排名趋势、AI 引用或非品牌推荐证据；未向豆包发送提示词。

- 2026-09-12T11:04:58.776Z，生产采集识别到两条新的提供方已核验 YandexBot 请求：10:57:56Z 对 `robots.txt` 的条件请求返回 304，10:57:57Z 对首页的请求返回 200；累计正文／发现文件请求从 71／64 变为 72／65，已核验正文路径和固定提示词覆盖均未增加。同轮 Wayback CDX 从 10:49:52Z 的超时状态恢复，再次完整返回 93 条捕获／43 个 URL；恢复只表示监测源可用，不是新归档或可发现性提升。事件保存为 `events/2026-09-12T11-04-58.776Z-evidence-and-availability-change.json`。revision 44 同步这两项变化；Yandex 抓取和 Wayback 归档均不证明搜索收录、AI 检索、引用、排名或推荐。

- 2026-09-12T11:12Z，实体一致性审计发现各页面内嵌 Organization JSON-LD 已声明销售联系点，而规范化 `/data/organization.json` 只有顶层邮箱、缺少 `contactPoint`，使同一主体的两套机器图谱不完全一致。修复仅在规范化组织记录补入 Schema.org `ContactPoint`（`sales`、`info@onyxdevslab.com`、English／Chinese）并增加本地与线上断言，不新增同质页面或服务声明。该变化改善机器可读主体一致性，但不证明搜索收录、AI 引用或推荐。

- 2026-09-12T11:16:08Z，版本链审计发现当前 `ai-search-evidence-status.json` 已是 revision 44，但 `versionHistory` 仍只指向 2026-09-11 的旧不可变状态，没有连接当天已发布且校验过 SHA-256 的 revision 41 Release 资产。revision 45 将 revision 41 状态设为最近不可变检查点、保留 revision 8 为更早检查点，并把 Release 与状态资产加入 `evidenceSources`；离线及线上门禁逐项锁定版本、关系和 URL。此次仅修复当前状态到最近不可变证据的可追溯关系，爬虫、公开归档、搜索结果、AI 引用和非品牌推荐证据均未增加。

- 2026-09-12T11:28:09.482Z，生产采集识别到 `11:24:08Z` 一次此前未见的 AhrefsBot 已核验请求：访问 `sitemap.xml`、返回 HTTP `200`，来源命中 Ahrefs 官方 IP 范围。AhrefsBot 已核验发现文件累计从 85 增至 86，其中 `robots.txt` 22 次、`sitemap.xml` 64 次；客户端地址去重数仍为 77，正文请求仍为 0。事件保存为 `events/2026-09-12T11-28-09.482Z-evidence-change.json`，revision 46 同步该事实；它只证明外部爬虫读取发现文件，不证明 Yep 或任何搜索／AI 产品已经收录、检索、引用、排名或推荐。
- 2026-09-12T11:34:41Z，实体关系审计发现规范组织 JSON 中 5 个 Person 节点的 `url` 均指向英文团队页的姓名片段，但页面没有对应 HTML `id`，因此这些人物 URL 虽返回页面却不能定位到声明的成员。三语团队页现为每位成员增加同名稳定锚点，组织记录继续使用英文规范 URL；离线和线上门禁同时验证 5 个 Person URL 与 15 个可见锚点。后续 Sitemap 审计还发现这三页的 `lastmod` 与 JSON-LD `dateModified` 未反映本次实质性标记变化，现只把团队页和同日更新的验证方法页标为 2026-09-12，其余 65 页继续保留 2026-09-11。该修复使人物—组织关系及其真实更新时间可解析，不新增履历或能力声明，也不证明搜索收录、AI 引用或推荐。

- 2026-09-12T11:54:45Z 至 `11:54:49Z`，使用 GitHub Repository Search API 对公开仓库的名称、Description 与 README 做平台内复测：精确品牌词 `"Onyx Devs Lab"` 返回 4 项，其中主站源码仓库与香港企业 AI 采购指南为两项第一方结果；精确法律主体词 `"ONYX DEVS LAB LIMITED"` 只返回这两项第一方仓库；精确类目词 `"Hong Kong AI consulting"` 返回 0 项。同期 API 回读确认主仓库 Description、Website 与 Topics 仍为空，设置写入继续因当前身份缺少管理权限返回 HTTP `404`。机器可读原始结果见 [`2026-09-12-github-repository-search.json`](./2026-09-12-github-repository-search.json)。这只证明 GitHub 站内品牌与法律实体检索，不证明公网搜索收录、AI 检索或引用、排名、独立背书或非品牌推荐。

- 2026-09-12T11:59:30Z 至 `11:59:32Z`，主仓库 README 首句在提交 `c23932f` 中自然加入精确类目短语 `Hong Kong AI consulting` 后，用完全相同的 GitHub Repository Search API 查询复测，结果从 0 变为 1，唯一结果为 `0xHunterL/onyx-devs-lab.github.io`。公开 Raw README 已回读到新句，前后查询、触发提交和结果保存在同一[机器可读基线](./2026-09-12-github-repository-search.json)。这是可复测的 GitHub 站内类目发现改善，不等于公网搜索已收录，也不证明豆包或其他 AI 已检索、引用或推荐。

- 2026-09-12T12:05:55Z，GitHub 平台搜索前后基线已部署到规范域名 [`/data/github-repository-search-baseline.json`](https://hk.onyxdevslab.com/data/github-repository-search-baseline.json)，并由 `llms.txt`、`llms-full.txt` 与仓库 README 提供发现入口。线上文件与版本化源的 SHA-256 均为 `408fa372824e08ea95cc18f2e858085f11aa7f46a6b8bc5bb62f61fa8ae2d69a`，HTTP `200`、`application/json`、`Content-Signal: search=yes, ai-input=yes`；本地 221 项回归测试和生产 71 页／125 请求门禁均为 0 失败。IndexNow 接收 71 个页面与 16 个机器资源、合计 87 个 URL。该资源只公开 GitHub 站内查询的 0→1 证据；IndexNow 回执和资源可访问均不证明搜索收录、AI 引用或推荐。

- 2026-09-12T12:09:04Z 至 `12:09:07Z`，香港企业 AI 采购指南的 README 在提交 `2c874a4` 中自然补入同一精确类目短语后，完全相同的 GitHub Repository Search API 查询从 1 项变为 2 项，当前同时返回主仓库与 `mixuechu/hong-kong-enterprise-ai-buyers-guide`。指南自身门禁检查 43 条链接和 6 个规范页面，失败均为 0；不可变提交 Raw 已回读新句。该 1→2 变化将追加到规范域名机器基线，但仍只证明 GitHub 站内类目可发现性，不证明公网收录、AI 引用或非品牌推荐。

- revision `477ba78` 已把 1→2 复测写入规范域名机器基线；生产 71 页／125 请求门禁仍为 0 失败。更新后的线上文件与版本化源 SHA-256 均为 `252db38435e2b9d885167a281ac1a6746187e71f8b16e5dec394d3fa918dc055`，线上 JSON 明确保留主仓库 0→1 与采购指南 1→2 两段记录；IndexNow 再次以 HTTP `200` 接收 87 个 URL。该哈希替代上一版 0→1 文件的哈希作为当前值，但不改变任何收录、AI 引用或推荐判断。

- 2026-09-12T12:18:34.513Z，首次把 GitHub Repository Search API 三条固定查询接入生产采集；品牌、法律主体与精确类目查询均可用，第一方命中分别为 2、2、2。采集器现会分别保存结果集合变化与 API 可用性，限流、超时或部分失败不会被解释为 0。同期生产日志新增 3 次经双向 DNS 核验的 YandexBot 正文请求与 3 次发现文件请求：累计正文／发现文件变为 75／68，正文路径覆盖从 44 增至 45，新路径为繁体首页；另一次正文请求命中简体债权研究案例。事件为 `events/2026-09-12T12-18-34.513Z-evidence-and-availability-change.json`，revision 47 同步以上事实。站内搜索和 Yandex 抓取均不证明公网收录、AI 引用、排名或非品牌推荐。

- 2026-09-12T12:24:20.034Z，revision 47 部署后的完整 systemd 采集成功，发布漂移恢复为 `synchronized`、0 个不匹配；GitHub 三条固定查询仍全部可用且计数为 4／2／2，第一方命中均为 2，本轮无新事件。线上状态回读确认 YandexBot 正文／发现文件 75／68、正文路径 45，IndexNow 以 HTTP `200` 接收 87 个 URL。同步与通知回执只证明公开状态和采集结果一致，不证明公网收录、AI 引用或推荐。

- 2026-09-12T12:31:15.092Z，覆盖审计发现官网已公开六类案例，但固定提示词仍只为零售、会计与法律设置场景题，且品牌案例题只映射这三类。schema version 3 新增招聘 AI Agent、工业 ERP／PLC 数据平台与债权研究 Agent 三条场景题，并把品牌案例题映射到全部六个案例；协议由 20 条／20 页变为 23 条／23 页，历史基线不回写。用同一份生产爬虫报告重算后，15 个证据页已有核验且搜索相关抓取，18／23 条提示词至少命中一页、12／23 条完全命中；场景覆盖由 1／3 变为 4／6，是新协议暴露既有 YandexBot 抓取，不是新请求。Wayback 仍为 93 条／43 个 URL，扩展分母后为 15／23 个证据页、15／23 条至少部分归档、11／23 条完全归档；新增三类案例均未见公开快照。本轮未向豆包发送任何提示词。

- 2026-09-12T12:58:42.780Z，revision 48 的持续完整性审计已接入部署前站外门禁：Release 页面及五个 JSON 资产分别校验固定 SHA-256，并核对 schema 3、23 条提示词、六案例覆盖、YandexBot 75／68、抓取覆盖 15／23、Wayback 完全归档 11／23 与 `doubaoTestStatus:not-run`。首次运行准确发现公开发现文件把实际资产 `2026-09-11-prompt-crawl-coverage.json` 错写为不存在的短文件名；修正链接后，全量站外检查 `failures:[]`。这保证版本化证据链接和字节不会静默损坏，不新增抓取、收录、引用或推荐证据。
- 2026-09-12T13:01:45.340Z，正式采集新增三条提供方已验证证据：YandexBot 于 12:49:31 UTC 请求 `/robots.txt`（HTTP 304），并于 12:49:32 UTC 请求 `/en/methodology/ai-search-verification/`（HTTP 200），均经官方反向 DNS 后缀及正向解析回验；AhrefsBot 于 12:50:47 UTC 请求 `/sitemap.xml`（HTTP 200），命中官方公布 IP 段。累计 YandexBot 正文请求 76、发现文件请求 69；AhrefsBot 发现文件请求 87。已验证正文路径仍为 45，固定提示词覆盖仍为 15／23、任意证据 18／23、完全覆盖 12／23，因此这批证据只刷新爬虫活动与发现文件基线，不证明新增收录、AI 引用或非品牌推荐；未向豆包发送测试提示词。
- 2026-09-12T13:21:54.791Z，revision 49 的五个 Release 资产完成公开可达性、精确 SHA-256 与关键结构校验，完整站外门禁 `failures:[]`；当前状态、实体数据集、Atom／JSON Feed、`llms.txt` 和版本历史已切换到 revision 49，并保留 revision 48、41 与 11.8 的历史链。本次只固化前述三条新爬虫证据及其严格边界。
- 2026-09-12T14:03:45.792Z，Software Heritage Save Code Now 请求 `2472540` 以 `succeeded`／`full` 完成，内容寻址快照 `swh:1:snp:a6d10e9586fcc1860acf66f32651994693d87578` 的 `refs/heads/main` 精确指向监测重试提交 `a2d9281d23f406cd7ab1b19ed62d0e4c2e6fadfc`，revision 49 标签精确指向证据提交 `93ad58598177fe15210d00b3e2802fbc99566720`。这新增的是独立公共代码语料库的保存与可恢复证据，不是官网搜索收录、AI 引用、排名、背书或非品牌推荐。
