# Onyx GEO 监测证据检查点：2026-09-11

本检查点固定保存生产机自动监测首次稳定运行后的当前证据。监测读取当前 Nginx GEO 日志以及 `.1`、`.2.gz` 数字轮转文件，并启用 OpenAI、Bing、Google 与 Perplexity 的提供方核验。

## 当前可复核结果

- 经提供方规则核验的 GPTBot 正文请求 24 次，覆盖多项企业 AI 采购与方法内容；GPTBot 属训练用途爬虫。
- 经官方地址段核验的 OAI-SearchBot `robots.txt` 请求累计 7 次，正文请求仍为 0；第 7 次发生在新问答关系部署后的 `2026-09-11T06:12:34Z`。同一秒 GPTBot 再次读取 `sitemap.xml`，但没有新增正文路径。
- 经双向 DNS 核验的 Bingbot 正文请求 7 次；Googlebot 与 Perplexity 正文请求均为 0。
- 固定 20 条提示词对应 20 个证据页，其中 8 页曾被已核验爬虫请求；与搜索／答案检索相关的已核验正文覆盖仍为 0。
- 归因请求 68 次，其中 59 次属于高速／多 User-Agent 协同批量访问、内部矛盾的浏览器身份或已知 VirusTotal 链接扫描，9 次访问者类型未验证；另有 6 个被 Cloudflare 邮箱解码路径污染的异常 campaign 值被审计并从归因总量排除。新一组 `github_discussions` 请求在 81 秒内由 4 个 User-Agent 扫过 3 个页面，10 次归入疑似自动访问；另 4 次使用不可能同时成立的现代 Chromium 与旧 EdgeHTML 身份。AI Referrer 为 0。
- 2026-09-11 07:31 UTC 复测唯一标识、品牌词、`site:` 和新 GitHub Discussion 精确标题，未观察到官网或新问答结果，也没有非品牌推荐。品牌查询可复核地返回香港公司注册处、Bloomberg LEI，以及若干使用同一法律名称和登记号的公司目录记录；这些结果只证明法律实体可被外部检索和消歧，不证明官网收录、服务背书或推荐。
- 未向豆包发送提示词。

机器可读数据见 [`2026-09-11-monitor-evidence.json`](./2026-09-11-monitor-evidence.json)。这些数字只区分可访问、已核验抓取候选及归因请求，不证明搜索收录、AI 检索、引用、排名、真人访问或非品牌推荐。
