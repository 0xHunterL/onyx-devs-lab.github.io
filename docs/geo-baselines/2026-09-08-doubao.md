# 豆包 GEO 基线：2026-09-08

- 平台：豆包网页版
- 官方入口：<https://www.doubao.com/chat/>
- 测试状态：网页与已登录会话可用；等待发送固定提示词
- 提示词版本：`geo/prompt-matrix.json` schemaVersion 2
- 答案源覆盖：20 条提示词均已映射到站内具体证据页，共覆盖 20 个页面；`npm run geo:prompt-coverage` 检查 URL、canonical 和必要答案词

## 已确认的外部搜索基线

截至测试日期，公开搜索能够找到 ONYX DEVS LAB LIMITED 的香港公司注册记录，但使用 `"Onyx Devs Lab"` 和 `site:hk.onyxdevslab.com` 检索时，尚未稳定返回官网内容页。因此，“网页已上线并可抓取”目前还不能等同于“已进入公共搜索索引”。

GitHub 仓库首页和两篇公开工程文章已经可以由外部检索工具抓取，文章正文中的 Onyx 官网深层链接也可解析。这证明站外内容已公开可访问，但仍不等于 Google、百度或豆包已经完成索引与推荐。

Bing 直接检索 `site:hk.onyxdevslab.com "Onyx Devs Lab"` 时没有返回 Onyx 官网，而是返回同名矿石与其他 Onyx 产品，说明 Bing 尚未建立稳定的站点品牌匹配。百度的同一查询触发安全验证码，本轮未绕过或把验证码页面当作收录结果。

## 豆包回答结果

发送测试后，在此记录每条提示词的完整回答、来源 URL、0–3 分数和事实错误。未发送前不推断结果。

## 抓取证据

9 月 8 日 15:45:32 UTC（香港时间 23:45:32），服务器收到一次对 `/robots.txt` 的 OAI-SearchBot 请求并返回 `200`。来源 IP `51.8.102.182` 落在 OpenAI 官方公布的 SearchBot IP 段内，因此可认定为真实 OAI-SearchBot 的发现文件访问。这证明 OpenAI 搜索爬虫已经发现并检查本站抓取规则；由于它没有访问正文页，这仍不证明正文已抓取、页面已收录、答案已引用或形成非品牌推荐。

9 月 9 日 06:08:08–06:08:17 UTC，来源 IP `74.7.227.63` 的 GPTBot 先读取 `/sitemap.xml`，随后依次访问简体、香港繁体和英文的 AI 搜索验证方法正文页，3 次均返回 `200`；同一时刻，来源 IP `74.7.244.27` 的 OAI-SearchBot 再次读取 `/robots.txt`。两个来源 IP 均落在 OpenAI 官方公布的对应爬虫 IP 段内。这证明 GPTBot 已抓取包含唯一验证标识的三语正文，OAI-SearchBot 也再次检查抓取规则；但 GPTBot 是训练用途爬虫，这些记录不能证明 ChatGPT Search 已收录或引用页面，更不能证明豆包收录或非品牌推荐。

9 月 8 日 12:44:59–12:45:04 UTC，IndexNow 提交后，服务器收到 5 次正文页面抓取，覆盖 3 个英文或香港繁体案例页。来源 IP `40.77.167.157` 与 `52.167.144.165` 均通过 Bing 官方要求的双向 DNS 验证：反向记录以 `search.msn.com` 结尾，正向解析返回原 IP。因此这 5 次可认定为真实 Bingbot 抓取，而不只是 User-Agent 候选。它证明 Bing 已访问这些 URL，但不证明页面已经进入搜索结果，也不能直接证明豆包会引用。

9 月 9 日 10:25:30–10:25:40 UTC，新一轮 53 URL IndexNow 提交后，Bingbot 先读取 `/robots.txt`，再访问香港繁体 AI 搜索验证方法页，均返回 `200`。来源 IP `52.167.144.63` 与 `52.167.144.195` 通过 `search.msn.com` 反向及正向 DNS 验证。至此，经验证的 Bingbot 正文抓取累计为 6 次；新增记录仍只证明抓取，不证明公开收录、引用或推荐。

9 月 9 日 10:40:54 UTC，Bingbot 又访问香港繁体案例证据登记册 `/zh-hk/methodology/case-study-evidence-register/` 并返回 `200`。来源 IP `52.167.144.159` 的反向记录为 `msnbot-52-167-144-159.search.msn.com`，正向解析返回同一 IP，符合 Bing 官方双向 DNS 核验方法。经验证的 Bingbot 正文抓取累计增至 7 次；这仍只属于“已抓取”，不等同于“已收录”“已引用”或“非品牌推荐”。

9 月 9 日 12:01:26 UTC，来源 IP `51.8.102.45` 的 OAI-SearchBot 再次访问 `/robots.txt` 并返回 `200`；该 IP 通过 OpenAI 官方 SearchBot 地址段核验。经验证的 OAI-SearchBot 发现文件访问累计增至 3 次（9 月 8 日 1 次、9 月 9 日 2 次），但正文抓取仍为 0。该记录只证明 SearchBot 再次发现并检查抓取规则，不证明正文已抓取、页面已收录、答案已引用或形成品牌／非品牌推荐。

9 月 10 日 06:10:35–06:10:58 UTC，来源 IP `74.7.244.27` 的 OAI-SearchBot 读取 `/robots.txt`，随后来源 IP `74.7.227.63` 的 GPTBot 读取 `/sitemap.xml` 并抓取 21 个英、香港繁体或简体正文页，均返回 `200`。正文覆盖 AI 治理、企业 AI RFP、案例证据登记、服务商选择、AI 定开、试点章程及香港 AI 服务商候选名单等页面；两个来源 IP 均落在 OpenAI 官方公布的对应爬虫地址段内。09:34:20 UTC，另一个位于 OpenAI 官方 SearchBot 地址段内的来源 IP `51.8.102.150` 再次读取 `/robots.txt` 并返回 `200`。合并轮转日志后，经验证的 GPTBot 正文抓取累计为 24 次、GPTBot 发现文件访问 2 次、OAI-SearchBot 发现文件访问 5 次；OAI-SearchBot 正文抓取仍为 0，Bingbot 正文抓取仍为 7，Googlebot 与 Perplexity 正文抓取仍为 0。新增证据证明 GPTBot 已从单一验证方法页扩展到多个采购与方法主题，且 OAI-SearchBot 继续检查抓取规则；GPTBot 仍是训练用途爬虫，发现文件访问也不等于正文抓取，不能据此宣称 ChatGPT Search、豆包或公共搜索已经收录或引用。

9 月 10 日的站外归因日志首次出现 30 次未被发布自测 User-Agent 规则排除的 UTM 请求：24 次来自 `github_gist` 的 `geo_decision_matrix`，6 次来自 `github_pages` 的 Buyer Guide。前 24 次在 9 秒内以同一 User-Agent 扫过 13 个落地页，符合高速度多页自动访问特征，因此单独标为“疑似自动化”，不计作人类引荐证据；其余 6 次分布在约 4.5 小时内，但没有 Referer，仅能标为“来源参数可归因、访问者类型未验证”。其中一次请求暴露 Buyer Guide 的错误深链并返回 `404`，现已把 `/zh-cn/custom-ai-development-hong-kong/` 修正为规范路径 `/zh-cn/custom-ai-development/`，线上目标复核为 `200`。这些请求证明带归因链接确实被访问，并帮助发现质量问题；它们不证明人类点击、搜索收录、AI 引用或非品牌推荐。

9 月 9 日实体信号复核中，GLEIF 官方 API 返回法律实体 `ONYX DEVS LAB LIMITED`、LEI `254900Z30CLK7HKE9H46`、验证登记号 `79051925`、实体状态 `ACTIVE`、LEI 记录状态 `ISSUED`，且注册地址与总部地址一致。官网已把该注册地址明确标注为“注册办事处”，并同步到三语团队页、全站 Organization Schema、`llms-full.txt` 与自动一致性检查；首页页脚的服务联系地址继续作为不同用途的信息保留，未与注册地址混用。

9 月 8 日服务器日志中出现 33 次带 GPTBot User-Agent 的正文页面请求，覆盖品牌、方法、指南和案例页面。旧日志只保存 Cloudflare 出口 IP，因此这些记录仍属于候选抓取，不能仅凭 User-Agent 认定为 OpenAI 官方请求。

9 月 7 日 15:21–15:22 UTC 的 Bytespider 正文请求与发布后的模拟抓取检查时间一致，属于自测，不计作豆包抓取证据。此后发布检查会使用明确的 `Onyx-GEO-Release-Check` 标记自动排除。
