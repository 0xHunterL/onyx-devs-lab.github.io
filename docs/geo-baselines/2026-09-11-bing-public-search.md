# Bing 公开搜索可复测检查点：2026-09-11

本检查点使用 Bing 公开 RSS 搜索接口复测四个固定查询：唯一检索标识、品牌词、`site:` 品牌词和站外 GitHub Discussion 精确标题。四次响应均为 HTTP `200`，每次观察到 10 个结果；声明的官网或 Discussion 目标 URL 均未出现。

报告只保存查询回显、结果数量、目标是否出现，以及排序后结果 URL 集合的 SHA-256 指纹。它不复制 Bing 的结果标题、摘要或 URL 清单。可用同一仓库命令复测：

```bash
npm run geo:public-search-report
```

前三个查询返回了完全相同的结果 URL 集合指纹 `0a226faa08df31826f8edb3e62b5f98382715b1062538b78c2ec545c85bb4093`。这表明该匿名 RSS 表面对品牌词、唯一标识和 `site:` 查询没有给出可区分的结果集合；因此只能记录“本次观察集合未出现目标”，不能据此断言 Bing 全局未收录、已经删除索引，或其他地区、界面和账户也没有结果。

`2026-09-11T11:40:30.196Z` 在生产机 `mxc-new` 独立运行同一报告，四项查询仍全部可用且目标命中为 0。前三项再次共享同一指纹 `3f84520931b92f28e1a1dd712e48379e25e4cc0a341e3ed9a0600444218e4cf4`，但它与本机指纹不同；Discussion 查询的生产指纹为 `ea3eba830024a882771d5da8fdd49869aed4018c771e9757ce6ec8be326cd792`。地点或时刻改变后结果集合发生变化，而目标未命中的窄结论一致，这进一步证明单次结果不能外推为全局索引状态。

机器可读证据见 [`2026-09-11-bing-public-search.json`](./2026-09-11-bing-public-search.json)。本结果不证明 Google、百度或豆包的收录状态，也不证明 AI 检索、引用、排名或非品牌推荐。本轮没有向豆包发送提示词，也没有处理验证码。
